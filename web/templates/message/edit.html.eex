<h2>Edit message</h2>

<%= render "form.html", changeset: @changeset,
                        action: document_message_path(@conn, :update, @document, @message) %>

<%= link "Back", to: document_message_path(@conn, :index, @document) %>
